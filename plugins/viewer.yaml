name: viewer
title: Amenbo Viewer
desc: Read your tasks on your phone, from a server you own
author: Amenbo
repo: ShiroDoromoto/amenbo-plugin-viewer
os:
  - macos
  - windows
  - linux
category: integration
scope: machine
assets:
  macos-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v4/viewer-v4-macos-arm64.tar.gz
    checksum: sha256:41d59a3c323778cf7504165c42d053bbd47ca1ceff0736a936fedefaf7458273
  macos-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v4/viewer-v4-macos-x64.tar.gz
    checksum: sha256:fe114126f0f0e5ea26a7363a7707f8081b8a7b00e205907015be9d1bb948323c
  linux-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v4/viewer-v4-linux-x64.tar.gz
    checksum: sha256:4f2003e25f31a3fd2ad59a625fdf81b76830a1c92d82773844b3f716abd46f45
  linux-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v4/viewer-v4-linux-arm64.tar.gz
    checksum: sha256:b17dbdfa0785f1ce560873d8811049746e166241d314e2fe9d4baf3e3fd4b27c
  windows-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v4/viewer-v4-windows-x64.tar.gz
    checksum: sha256:8ea2671fb6890c3d9aabf7b640a756825db489b2ee7e8de0903b90d620596fb8
  windows-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v4/viewer-v4-windows-arm64.tar.gz
    checksum: sha256:df277dafec618677c89f06215b0b155f90823263e905e53f41280769ee69b871
official: true
payload_v: 1
min_amenbo: 15.1.0
about: |
  Your tasks reach your phone, and the Amenbo Viewer app reads them there — the tasks, the state each one is in, and the decisions behind them — while you are away from the PC.

  Nothing is hosted for you. The server is one you own: a server and a database that "3. Set up the server on Cloudflare" makes in your own Cloudflare account. Tasks travel one way, and only as they move.

  They travel sealed. "4. Pair a phone" pairs a phone off the network — the camera reads the server's URL, the read API token and the encryption key. "5. See the phones that are paired" says which phones may read, and "6. Unpair a phone" cuts one of them off.

  Enabling it carries every project on this machine: a phone is one device, not one project.
config:
  - key: routes
    label: Where to carry
    help: |-
      Whether this may carry to your own server on Cloudflare. It can only take the place away: ticking it still needs the server to exist before anything reaches it, and "3. Set up the server on Cloudflare" is what makes one. Ticking nothing is an answer and it is honoured: the plugin stays enabled and carries nowhere.
    type: multi
    options:
      - value: cloudflare
        label: Your own server on Cloudflare
    default: cloudflare
    required: false
  - key: worker_url
    label: Your server's URL on Cloudflare
    help: |-
      "3. Set up the server on Cloudflare" writes this when it makes the server. There is nothing to type.
    required: false
    readonly: true
  - key: auth_token
    label: API token the server takes writes with
    help: |-
      "3. Set up the server on Cloudflare" generates this. There is nothing to type.
    secret: true
    required: false
    readonly: true
  - key: encryption_key
    label: Key your tasks are encrypted with
    help: |-
      "3. Set up the server on Cloudflare" generates this. There is nothing to type - the phone is given it by the QR, never over the network.
    secret: true
    required: false
    readonly: true
settings:
  check: check
  actions:
    - cmd: app
      label: 1. Get the app on your phone
    - cmd: token
      label: 2. Get a Cloudflare API token
    - cmd: setup
      label: 3. Set up the server on Cloudflare
      ask:
        - key: api_token
          label: Cloudflare API token
          secret: true
    - cmd: qr
      label: 4. Pair a phone
      ask:
        - key: label
          label: What to call this phone
    - cmd: phones
      label: 5. See the phones that are paired
    - cmd: revoke
      label: 6. Unpair a phone
      ask:
        - key: phone
          label: The name of the phone to unpair
events:
  - task.created
  - task.status_changed
  - task.done
  - task.rejected
  - task.assigned
  - task.moved
  - task.deleted
  - decision.accepted
  - decision.rejected
  - comment.added
  - comment.removed
  - store.changed
agent:
  when: Setting up the phone viewer, pairing a phone with it, or carrying by hand what has not reached one yet
  commands:
    - cmd: app
      does: Puts the App Store page for Amenbo Viewer on a QR the phone's camera reads, so the address does not have to be carried from this screen to the phone by hand. Asks nothing and keeps nothing
    - cmd: token
      does: Opens Cloudflare's token screen in the browser, with the three permissions setup needs already ticked, and ends there. Asks nothing and keeps nothing — the token the page makes is pasted into setup
    - cmd: setup
      does: Stands up the Worker and its database in the user's own Cloudflare account, and generates the token and key. Takes a Cloudflare API token from the settings screen, a terminal or CLOUDFLARE_API_TOKEN
    - cmd: push
      does: Carries by hand what has not reached the phone yet, encrypting a record at a time, over every route that is configured. The hook does this per write, so this is for what was left behind
    - cmd: phones
      does: Lists the phones that may read and when each was paired. Reads nothing over the network — the store compares hashes and does not answer what it holds
    - cmd: revoke
      does: Cuts one phone off by the name it was paired under, at the store first and here after. The other phones carry on reading. Takes that name as its only argument, or out of the settings screen's box
    - cmd: qr
      does: "Pairs one phone: issues a read token, registers its hash, puts endpoint/token/key/name on a QR the camera reads, off the network. --label or the settings screen box names it; --terminal draws it here"
    - cmd: check
      does: Says where records are reaching right now — the places ticked under 'Where to carry' that also exist. Reads nothing over the network; the settings screen raises it, and a terminal may too
