name: viewer
desc: Read your backlog on your phone, from a place you own
author: Amenbo
repo: ShiroDoromoto/amenbo-plugin-viewer
os:
  - macos
  - windows
  - linux
category: integration
scope: machine
assets:
  macos-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v2/viewer-v2-macos-arm64.tar.gz
    checksum: sha256:2f83f0836bbd8f41fe0b6cd09a472b7f4ce9f555605b579ae84b0be83cbbd4cd
  macos-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v2/viewer-v2-macos-x64.tar.gz
    checksum: sha256:181db648bd04987a95a8f83ecf42961bc367d39f965f2e416f4b356c09c0fe6c
  linux-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v2/viewer-v2-linux-x64.tar.gz
    checksum: sha256:0f5713ec3264664d29f477ff1990a5c657acf5a72f4b97c5a77ad14e2847fe82
  linux-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v2/viewer-v2-linux-arm64.tar.gz
    checksum: sha256:3e7dc83420652877ebaf644d3f34c63cbbcb7ed74fb46ac83e5ecbb6bdd826fb
  windows-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v2/viewer-v2-windows-x64.tar.gz
    checksum: sha256:edb50825b53b751007c48cd7ed2ba1defade80c251360d2393a97449ccb437bf
  windows-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v2/viewer-v2-windows-arm64.tar.gz
    checksum: sha256:8f233720af30cf81b98802a2c6b87da3a8cb0b6411d7e7464362fbd6f8f86d7e
official: true
payload_v: 1
min_amenbo: 13.0.0
about: |
  Your backlog reaches your phone, and the Amenbo Viewer app reads it there — the tasks, the state each one is in, and the decisions behind them — while you are away from the PC.

  Nothing is hosted for you. The place is one you own: an app-private iCloud folder on a Mac, or a Cloudflare Worker and database that `setup` stands up in your own Cloudflare account. Records travel one way, and only as they move.

  On the Cloudflare route they travel sealed. `qr` pairs a phone off the network — the camera reads the endpoint, the token and the key. `phones` says which phones may read, and `revoke` cuts one of them off.

  Enabling it carries every project on this machine: a phone is one device, not one project.
config:
  - key: routes
    label: Where to carry
    help: |-
      Which places this may carry to. It can only take places away: a place you tick still needs to exist before anything reaches it — the iCloud folder appears once you have opened Amenbo Viewer on a phone (the "Get the app on your phone" button puts its App Store page on a code), and the Cloudflare one is what setup stands up. Ticking none is an answer and it is honoured: the plugin stays enabled and carries nowhere.
    type: multi
    options:
      - value: icloud
        label: The app's own iCloud folder
      - value: cloudflare
        label: Your own Cloudflare Worker
    default: icloud,cloudflare
    required: false
  - key: worker_url
    label: Your Cloudflare Worker's URL
    help: setup writes this when it stands the Worker up. There is nothing to type.
    required: false
    readonly: true
  - key: auth_token
    label: Token the Worker lets writes in with
    help: setup generates this. There is nothing to type.
    secret: true
    required: false
    readonly: true
  - key: encryption_key
    label: Key the records are encrypted with
    help: setup generates this. There is nothing to type - the phone is given it by the QR, never over the network.
    secret: true
    required: false
    readonly: true
settings:
  check: check
  actions:
    - cmd: app
      label: 1. Get the app on your phone
    - cmd: token
      label: 2. Get a Cloudflare API token
    - cmd: setup
      label: 3. Set up the Cloudflare route
      ask:
        - key: api_token
          label: Cloudflare API token
          secret: true
    - cmd: qr
      label: 4. Pair a phone
      ask:
        - key: label
          label: What to call this phone
events:
  - task.created
  - task.status_changed
  - task.done
  - task.rejected
  - task.assigned
  - task.moved
  - task.deleted
  - decision.accepted
  - decision.rejected
  - comment.added
  - comment.removed
  - store.changed
agent:
  when: Setting up the phone viewer, pairing a phone with it, or carrying by hand what has not reached one yet
  commands:
    - cmd: app
      does: Puts the App Store page for Amenbo Viewer on a QR the phone's camera reads, so the address does not have to be carried from this screen to the phone by hand. Asks nothing and keeps nothing
    - cmd: token
      does: Opens Cloudflare's token screen in the browser, with the three permissions setup needs already ticked, and ends there. Asks nothing and keeps nothing — the token the page makes is pasted into setup
    - cmd: setup
      does: Stands up the Worker and its database in the user's own Cloudflare account, and generates the token and key. Takes a Cloudflare API token from the settings screen, a terminal or CLOUDFLARE_API_TOKEN
    - cmd: push
      does: Carries by hand what has not reached the phone yet, encrypting a record at a time, over every route that is configured. The hook does this per write, so this is for what was left behind
    - cmd: phones
      does: Lists the phones that may read and when each was paired. Reads nothing over the network — the store compares hashes and does not answer what it holds
    - cmd: revoke
      does: Cuts one phone off by the name it was paired under, at the store first and here after. The other phones carry on reading. Takes that name as its only argument
    - cmd: qr
      does: "Pairs one phone: issues a read token, registers its hash, puts endpoint/token/key/name on a QR the camera reads, off the network. --label or the settings screen box names it; --terminal draws it here"
    - cmd: check
      does: Says where records are reaching right now — the places ticked under 'Where to carry' that also exist. Reads nothing over the network; the settings screen raises it, and a terminal may too
