name: viewer
title: Amenbo Viewer
desc: Read your backlog on your phone, from a place you own
author: Amenbo
repo: ShiroDoromoto/amenbo-plugin-viewer
os:
  - macos
  - windows
  - linux
category: integration
scope: machine
assets:
  macos-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v3/viewer-v3-macos-arm64.tar.gz
    checksum: sha256:17ae59113276fd8844517f7a25c1f1f9f7815bee8b89532ae64f62c2b9045456
  macos-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v3/viewer-v3-macos-x64.tar.gz
    checksum: sha256:7b345fe3255033577ef270edddbf3054b480f7fc724a127746807862a4bc98a2
  linux-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v3/viewer-v3-linux-x64.tar.gz
    checksum: sha256:37c5912ebc224eeeefebb8da3904e12c2b75e02c30675bea609fefc4d7526540
  linux-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v3/viewer-v3-linux-arm64.tar.gz
    checksum: sha256:873b4d4c73967dcf9df9e3ee3c64048fb38e2006761ee8f5fd747e3a21a84dd2
  windows-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v3/viewer-v3-windows-x64.tar.gz
    checksum: sha256:2becb8acbe732e25368a10958bc1e15ea6b43156ecde347193b704dedbdb3681
  windows-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v3/viewer-v3-windows-arm64.tar.gz
    checksum: sha256:fe825c1315561201431f75d8649424c0d7321c1acec7d19bc69f265ea5fb7776
official: true
payload_v: 1
min_amenbo: 13.0.0
about: |
  Your backlog reaches your phone, and the Amenbo Viewer app reads it there — the tasks, the state each one is in, and the decisions behind them — while you are away from the PC.

  Nothing is hosted for you. The place is one you own: a Cloudflare Worker and database that `setup` stands up in your own Cloudflare account. Records travel one way, and only as they move.

  They travel sealed. `qr` pairs a phone off the network — the camera reads the endpoint, the token and the key. `phones` says which phones may read, and `revoke` cuts one of them off.

  Enabling it carries every project on this machine: a phone is one device, not one project.
config:
  - key: routes
    label: Where to carry
    help: |-
      Whether this may carry to your Cloudflare Worker. It can only take the place away: ticking it still needs the Worker to exist before anything reaches it, and setup is what stands one up. Ticking nothing is an answer and it is honoured: the plugin stays enabled and carries nowhere.
    type: multi
    options:
      - value: cloudflare
        label: Your own Cloudflare Worker
    default: cloudflare
    required: false
  - key: worker_url
    label: Your Cloudflare Worker's URL
    help: setup writes this when it stands the Worker up. There is nothing to type.
    required: false
    readonly: true
  - key: auth_token
    label: Token the Worker lets writes in with
    help: setup generates this. There is nothing to type.
    secret: true
    required: false
    readonly: true
  - key: encryption_key
    label: Key the records are encrypted with
    help: setup generates this. There is nothing to type - the phone is given it by the QR, never over the network.
    secret: true
    required: false
    readonly: true
settings:
  check: check
  actions:
    - cmd: app
      label: 1. Get the app on your phone
    - cmd: token
      label: 2. Get a Cloudflare API token
    - cmd: setup
      label: 3. Set up the Cloudflare route
      ask:
        - key: api_token
          label: Cloudflare API token
          secret: true
    - cmd: qr
      label: 4. Pair a phone
      ask:
        - key: label
          label: What to call this phone
events:
  - task.created
  - task.status_changed
  - task.done
  - task.rejected
  - task.assigned
  - task.moved
  - task.deleted
  - decision.accepted
  - decision.rejected
  - comment.added
  - comment.removed
  - store.changed
agent:
  when: Setting up the phone viewer, pairing a phone with it, or carrying by hand what has not reached one yet
  commands:
    - cmd: app
      does: Puts the App Store page for Amenbo Viewer on a QR the phone's camera reads, so the address does not have to be carried from this screen to the phone by hand. Asks nothing and keeps nothing
    - cmd: token
      does: Opens Cloudflare's token screen in the browser, with the three permissions setup needs already ticked, and ends there. Asks nothing and keeps nothing — the token the page makes is pasted into setup
    - cmd: setup
      does: Stands up the Worker and its database in the user's own Cloudflare account, and generates the token and key. Takes a Cloudflare API token from the settings screen, a terminal or CLOUDFLARE_API_TOKEN
    - cmd: push
      does: Carries by hand what has not reached the phone yet, encrypting a record at a time, over every route that is configured. The hook does this per write, so this is for what was left behind
    - cmd: phones
      does: Lists the phones that may read and when each was paired. Reads nothing over the network — the store compares hashes and does not answer what it holds
    - cmd: revoke
      does: Cuts one phone off by the name it was paired under, at the store first and here after. The other phones carry on reading. Takes that name as its only argument
    - cmd: qr
      does: "Pairs one phone: issues a read token, registers its hash, puts endpoint/token/key/name on a QR the camera reads, off the network. --label or the settings screen box names it; --terminal draws it here"
    - cmd: check
      does: Says where records are reaching right now — the places ticked under 'Where to carry' that also exist. Reads nothing over the network; the settings screen raises it, and a terminal may too
