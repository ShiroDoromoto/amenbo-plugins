name: viewer
title: Amenbo Viewer
desc: Read your tasks on your phone, from a server you own
author: Amenbo
repo: ShiroDoromoto/amenbo-plugin-viewer
os:
  - macos
  - windows
  - linux
category: integration
scope: machine
assets:
  macos-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v8/viewer-v8-macos-arm64.tar.gz
    checksum: sha256:8158e45f01923b4128c476b25ae7d8b5a0daf7aebad7ec2e590d7741945e18d0
  macos-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v8/viewer-v8-macos-x64.tar.gz
    checksum: sha256:acf80b1d94ce242ff86579fe70d6fe384bc2a558cf1a0aaf8735b9433a343104
  linux-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v8/viewer-v8-linux-x64.tar.gz
    checksum: sha256:9ea0118bb98ef764e4d0f15753b31787d3f0cd8a5a2e6a285cb8dc75ccadc7f2
  linux-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v8/viewer-v8-linux-arm64.tar.gz
    checksum: sha256:ecee55ae6105f8973c7ad6715b8cd9e85360794334c15c8d74b91030dd077e04
  windows-x64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v8/viewer-v8-windows-x64.tar.gz
    checksum: sha256:9120c26d14f75b9db4436953ffcd51faa876f05ec9289f5420697f2485be6c26
  windows-arm64:
    url: https://github.com/ShiroDoromoto/amenbo-plugin-viewer/releases/download/v8/viewer-v8-windows-arm64.tar.gz
    checksum: sha256:290875733ef8ab1e0ae50cd7d4833a2670e6020515e6ea1c8d3c2721b20d2294
official: true
payload_v: 1
min_amenbo: 15.1.0
about: |
  Your tasks reach your phone, and the Amenbo Viewer app reads them there — the tasks, the state each one is in, and the decisions behind them — while you are away from the PC.

  Nothing is hosted for you. The server is one you own: a server and a database that "3. Set up the server on Cloudflare" makes in your own Cloudflare account. Tasks travel one way, and only as they move.

  They travel sealed. "4. Pair a phone" pairs a phone off the network — the camera reads the server's URL, the read code and the encryption key. "5. See whether a phone is paired" says whether one may read, and "6. Undo the pairing" takes that code away.

  Enabling it carries every project on this machine: a phone is one device, not one project.
config:
  - key: routes
    label: Where to carry
    help: |-
      Whether this may carry to your own server on Cloudflare. It can only take the place away: ticking it still needs the server to exist before anything reaches it, and "3. Set up the server on Cloudflare" is what makes one. Ticking nothing is an answer and it is honoured: the plugin stays enabled and carries nowhere.
    type: multi
    options:
      - value: cloudflare
        label: Your own server on Cloudflare
    default: cloudflare
    required: false
  - key: worker_url
    label: Your server's URL on Cloudflare
    help: |-
      "3. Set up the server on Cloudflare" writes this when it makes the server. There is nothing to type.
    required: false
    readonly: true
  - key: auth_token
    label: API token the server takes writes with
    help: |-
      "3. Set up the server on Cloudflare" generates this. There is nothing to type.
    secret: true
    required: false
    readonly: true
  - key: encryption_key
    label: Key your tasks are encrypted with
    help: |-
      "3. Set up the server on Cloudflare" generates this. There is nothing to type - the phone is given it by the QR, never over the network.
    secret: true
    required: false
    readonly: true
settings:
  check: check
  actions:
    - cmd: app
      label: 1. Get the app on your phone
    - cmd: token
      label: 2. Get a Cloudflare API token
    - cmd: setup
      label: 3. Set up the server on Cloudflare
      ask:
        - key: api_token
          label: Cloudflare API token
          secret: true
    - cmd: qr
      label: 4. Pair a phone
    - cmd: phones
      label: 5. See whether a phone is paired
    - cmd: revoke
      label: 6. Undo the pairing
    - cmd: repair
      label: 7. Fix the phone's copy
events:
  - task.created
  - task.status_changed
  - task.done
  - task.rejected
  - task.assigned
  - task.moved
  - task.deleted
  - decision.accepted
  - decision.rejected
  - comment.added
  - comment.removed
  - store.changed
agent:
  when: Setting up the phone viewer, pairing a phone with it, or carrying by hand what has not reached one yet
  commands:
    - cmd: app
      does: Puts the store page for Amenbo Viewer on a QR the phone's camera reads — one code for iPhone, one for Android — so the address is not carried to the phone by hand. Asks nothing and keeps nothing
    - cmd: token
      does: Opens Cloudflare's token screen in the browser, with the three permissions setup needs already ticked, and ends there. Asks nothing and keeps nothing — the token the page makes is pasted into setup
    - cmd: setup
      does: Stands up the Worker and its database in the user's own Cloudflare account, and generates the token and key. Takes a Cloudflare API token from the settings screen, a terminal or CLOUDFLARE_API_TOKEN
    - cmd: push
      does: Carries by hand what has not reached the phone yet, encrypting a record at a time, over every route that is configured. The hook does this per write, so this is for what was left behind
    - cmd: phones
      does: Says whether a phone may read, and since when, by asking the store. There is one read code, so the answer is present or absent rather than a list
    - cmd: revoke
      does: "Undoes the pairing: takes the read code away, so whatever was holding it stops reading. Takes nothing — there is one code, so there is nothing to name"
    - cmd: qr
      does: "Pairs a phone: issues the read code, registers its hash, puts endpoint/code/key on a QR the camera reads, off the network. Issuing again replaces it; --terminal draws the code here"
    - cmd: repair
      does: Compares what the phone's server holds with what this PC holds and carries the difference, without emptying anything. Counts on the first run and sends on the second — or on the first with --send
    - cmd: check
      does: Says where records are reaching right now — the places ticked under 'Where to carry' that also exist. Reads nothing over the network; the settings screen raises it, and a terminal may too
