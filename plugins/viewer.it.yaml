desc: Leggi il tuo elenco di attività sul telefono, da un posto che è tuo
config:
  worker_url:
    label: URL del tuo Cloudflare Worker (lo inserisce setup)
  auth_token:
    label: Token con cui il Worker accetta le scritture (lo inserisce setup)
  encryption_key:
    label: Chiave con cui i record vengono cifrati (la inserisce setup)
