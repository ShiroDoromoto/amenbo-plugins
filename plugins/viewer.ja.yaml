desc: バックログを、自前の置き場からスマートフォンで読む
about: |
  PC から離れているあいだも、バックログがスマートフォンへ届き、Amenbo Viewer で読める——タスクと、その一つ一つが今どの状態か、そして決めた理由が。

  置き場は運営が持たない。持つのは利用者で、mac ならアプリ専用の iCloud フォルダ、そうでなければ `setup` が自分の Cloudflare アカウントへ立てる Worker とデータベースになる。レコードは片方向に、動いたぶんだけ運ぶ。

  Cloudflare 経路では封をして運ぶ。`qr` はネットワークを通さずに端末をペアリングし、置き場とトークンと鍵をカメラへ渡す。読める端末は `phones` で分かり、`revoke` で1台だけ切れる。

  有効にすると、運ぶのはこの端末の全プロジェクトになる——スマートフォンは1台であって、1プロジェクトではない。
config:
  routes:
    label: どこへ運ぶか
    help: |-
      運んでよい置き場です。ここで選べるのは減らすことだけで、選んだ置き場もそこへ届くより先に実在している必要があります——iCloud のフォルダは Amenbo Viewer を端末で一度開くと現れ（「アプリを端末に入れる」のボタンが App Store のページを QR に載せます）、Cloudflare の置き場は setup が立てます。1つも選ばないことも答えで、そのまま尊重されます——有効なまま、どこへも運びません。
    options:
      icloud: アプリ専用の iCloud フォルダ
      cloudflare: 自分の Cloudflare Worker
  worker_url:
    label: 自分の Cloudflare Worker の URL
    help: "setup が Worker を立てるときに書き込みます。打つものはありません。"
  auth_token:
    label: Worker が書き込みを受け入れるトークン
    help: "setup が生成します。打つものはありません。"
  encryption_key:
    label: 記録を暗号化する鍵
    help: "setup が生成します。打つものはありません——端末へは QR で渡り、ネットワークには出ません。"
settings:
  actions:
    app:
      label: 1. アプリを端末に入れる
    token:
      label: 2. API トークンを取る
    setup:
      label: 3. Cloudflare 経路を立てる
      ask:
        api_token: Cloudflare の API トークン
    qr:
      label: 4. 端末をペアリング
      ask:
        label: この端末の名前
