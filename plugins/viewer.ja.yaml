desc: バックログを、自前の置き場からスマートフォンで読む
about: |
  PC から離れているあいだも、バックログがスマートフォンへ届き、Amenbo Viewer で読める——タスクと、その一つ一つが今どの状態か、そして決めた理由が。

  置き場は運営が持たない。持つのは利用者で、`setup` が自分の Cloudflare アカウントへ立てる Worker とデータベースになる。レコードは片方向に、動いたぶんだけ運ぶ。

  運ぶときは封をする。`qr` はネットワークを通さずに端末をペアリングし、置き場とトークンと鍵をカメラへ渡す。読める端末は `phones` で分かり、`revoke` で1台だけ切れる。

  有効にすると、運ぶのはこの端末の全プロジェクトになる——スマートフォンは1台であって、1プロジェクトではない。
config:
  routes:
    label: どこへ運ぶか
    help: |-
      自分の Cloudflare Worker へ運んでよいかどうかです。ここで選べるのは減らすことだけで、選んでも、そこへ届くより先に Worker が実在している必要があります——立てるのは setup です。選ばないことも答えで、そのまま尊重されます——有効なまま、どこへも運びません。
    options:
      cloudflare: 自分の Cloudflare Worker
  worker_url:
    label: 自分の Cloudflare Worker の URL
    help: "setup が Worker を立てるときに書き込みます。打つものはありません。"
  auth_token:
    label: Worker が書き込みを受け入れるトークン
    help: "setup が生成します。打つものはありません。"
  encryption_key:
    label: 記録を暗号化する鍵
    help: "setup が生成します。打つものはありません——端末へは QR で渡り、ネットワークには出ません。"
settings:
  actions:
    app:
      label: 1. アプリを端末に入れる
    token:
      label: 2. API トークンを取る
    setup:
      label: 3. Cloudflare 経路を立てる
      ask:
        api_token: Cloudflare の API トークン
    qr:
      label: 4. 端末をペアリング
      ask:
        label: この端末の名前
